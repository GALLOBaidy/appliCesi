-------BACK--------
npx tsc
npx ts-node-dev src/server.ts
npm install -D ts-node-dev
node dist/server.js
docker ps
docker start postgres_container
docker exec -it postgres_container psql -U user -d mydb
npx drizzle-kit migrate
npx drizzle-kit generate
docker compose up -d
npm install jsonwebtoken
npm install -D @types/jsonwebtoken
npm install -D vitest supertest ts-node @types/supertest
npm install react-native-toast-message
expo install @expo/vector-icons
npm install --save-dev @testing-library/react-native@12.4.5 @testing-library/jest-native@5.4.3 --legacy-peer-deps
npx expo install jest-expo
npm install -D vitest
npm install --save-dev @types/jest
npm install swagger-ui-express swagger-jsdoc

npm test

"login": "johnDoes",
"email": "john.doe@example.com",
"password": "Admin",


Devops
sudo apt update
sudo apt install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin


docker exec -t <nom_container_local> pg_dump -U user -d mydbAppli > dump.sql
scp -i C:\Users\black\Downloads\cesi_key.pem dump.sql azureuser@74.242.219.88:/var/www/applicesi/   dump.sql                                                            